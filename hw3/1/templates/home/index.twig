<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
</head>
<body>
    <p>Hello, {{name}}</p>
    <div>
        <ul>
            {% for  item in navigation %}
                <li><a href="{{ item.href }}"></a> {{ item.caption }}</li>
            {% endfor %}
        </ul>
    </div>
    
</body>
</html>